# Better Auth (required)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
# Use your production URL in production (e.g. https://your-domain.com)
BETTER_AUTH_URL=http://localhost:3000

# Optional: for client-side auth base URL (defaults to BETTER_AUTH_URL)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google OAuth – https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Discord OAuth – https://discord.com/developers/applications
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# MongoDB – https://mongodb.com
MONGO_URI=

# ESV API – https://api.esv.org/ (for Bible tab + verse of the day)
ESV_API_KEY=

# SMTP2GO – https://www.smtp2go.com/ (emails: OTP, reminders, weekly digest, grace-period warnings, accountability invites)
# Free tier: 100 emails/day, 2000/month (enforced in-app)
SMTP2GO_API_KEY=
SMTP2GO_FROM_EMAIL=Devo Tracker <noreply@your-domain.com>

# Cron secret – generate with: openssl rand -base64 32
# Required for /api/cron/* endpoints; set Authorization: Bearer <CRON_SECRET>
CRON_SECRET=